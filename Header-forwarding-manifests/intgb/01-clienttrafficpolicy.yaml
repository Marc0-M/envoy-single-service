apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: trust-all-xff
  namespace: common-gw-intgb
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: gw-intgb
  clientIPDetection:
    xForwardedFor:
      trustedCIDRs:
        - 0.0.0.0/0
        - ::/0
