# Replace metadata.name with the generated Envoy Service name for gw-accp.
# Example lookup:
# kubectl get svc -n envoy-system | grep "common-gw-accp"
apiVersion: v1
kind: Service
metadata:
  name: REPLACE_ME_WITH_GENERATED_ENVOY_SERVICE_NAME
  namespace: envoy-system
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-target-group-attributes: preserve_client_ip.enabled=true
